language: java
jdk:
  - oraclejdk9
install:
before_script:
  - chmod +x gradlew
script:
  - ./gradlew clean build
  - ./gradlew jacocoTestReport
after_success:
  - bash <(curl -s https://codecov.io/bash)
