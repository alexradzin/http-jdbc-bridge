select first_name, replace(credit_card, '\d{12}', '*') from people limit 100
select * from (table1, table2, /.+info$/) limit
select * from * join * group by * limit 1000
select people from * where id (=,in)

insert into * limit 200
update * limit 300